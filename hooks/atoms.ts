import {atom} from "recoil";

export const cursorTextState=atom({ 
key:"cursorTextState",
default:""
})

export const cursorVariantState=atom({ 
    key:"cursorVariantState",
    default:"default"
    })